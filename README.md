Creating Portfolio with EJS:

- "mkdir" a folder for all the files respectively before you could get them to work on your localhost:3000
- nodemon index.js and check your localhost for the final result. 
